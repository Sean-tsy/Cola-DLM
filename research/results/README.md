# 结果归档 (`research/results/`)

归档诊断 / 评测的结构化结果：指标表、对比 CSV、归因分析摘要。

约定：

- 只入库**小体积**的结构化结果（CSV / JSON / Markdown 摘要）。
- 大文件（原始生成 JSONL、权重、采样轨迹）**不入库**——放服务器侧或大文件存储
  （参见仓库根 `.gitignore`）。
- 每份结果标注：对应配置 (`research/configs/`)、权重版本、seed、生成时间，
  保证可追溯、可复现。

> 本环节仅建立骨架；具体结果在后续评测环节产出。
