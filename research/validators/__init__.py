"""验证器 / verifier (Cola DLM 诊断研究层)。

对模型生成结果（推理输出 JSONL 的 ``generate`` 字段）做**精确结构一致性**
校验，作为 RLVR 的可验证奖励信号来源：

* Dyck / 括号匹配是否闭合、嵌套正确
* 严格格式是否符合给定 schema
* 工具调用是否为合法、可解析的调用

验证器为纯函数式、确定性、纯 CPU，可在本地单测。具体实现在后续环节加入。
"""
