# env节九 9.4 · H3 block sweep: block=1 对照。
run_id: dyck_L64_D8_k1_blk1
task: dyck
n_samples: 256
seeds: [1234, 2025, 3407]

data:
  length: 64
  max_depth: 8
  k: 1
  mode: completion

sampling:
  block_size: 1
  timestep_num: 16
  guidance_scale: 7.0
  max_new_tokens: 96
  temperature: 0.0
  trace: true

checkpoint:
  dit: ${DIT_PATH}
  vae: ${VAE_PATH}
  tokenizer: ${TOKENIZER_PATH}
