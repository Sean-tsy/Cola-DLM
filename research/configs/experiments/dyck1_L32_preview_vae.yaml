# env节九 9.2/9.4 · H4/H5 spot check: VAE-only Dyck-1 L=32 reconstruction.
run_id: dyck1_L32_preview_vae
task: dyck
n_samples: 64
seeds: [1234, 2025]

data:
  length: 32
  max_depth: 8
  k: 1
  mode: unconditional

sampling:
  block_size: 4
  timestep_num: 16
  guidance_scale: 7.0
  max_new_tokens: 48
  temperature: 0.0
  trace: false

checkpoint:
  vae: ${VAE_PATH}
  tokenizer: ${TOKENIZER_PATH}
