# 严格格式（JSON）一致性诊断（L1）。
run_id: json_strict_blk4
task: structured
n_samples: 256
seeds: [1234, 2025, 7]

data:
  fmt: json
  max_depth: 2
  max_props: 4

sampling:
  block_size: 4
  timestep_num: 16
  guidance_scale: 7.0
  max_new_tokens: 96
  temperature: 0.0
  trace: true

checkpoint:
  dit: ${DIT_PATH}
  vae: ${VAE_PATH}
  tokenizer: ${TOKENIZER_PATH}
