# Cola-DLM 诊断实验交接记录（2026-06-10）

本文档整理当前 Cola-DLM 诊断实验的完成情况、服务器操作流程、注意事项和下一步顺序，供后续 agent 接手使用。

## 1. 当前状态总览

- 本地仓库：`/Users/tsy_02/Cola-DLM`
- 服务器仓库：`/data0/users/siyuan/projects/Cola-DLM`
- SSH 目标：`cityu_tecent`
- 当前分支：`research/diagnostics-scaffold`
- 当前本地 commit：`d5c820d`
- 上次确认服务器 commit：`d5c820d`
- 服务器 GPU 状态：最近一次 `nvidia-smi` 显示 8 张 GPU 均空闲。
- 当前原则：本地只跑纯 Python 骨架、验证器、指标和单测；所有模型、权重、GPU 推理与评测只在服务器跑。

## 2. 全局硬约束

- 不在本地加载模型，不在本地安装 torch，不执行 `pip install -e .`。
- 不改上游模型核心逻辑，除非用户明确要求。默认只在 `research/` 下新增或修改研究层代码。
- 不创建任何后训练相关目录或配置。本阶段只做诊断与评测，不做 SFT/DPO/RL/RLVR 实现。
- 服务器长任务必须 detached 后台运行，不能前台阻塞 SSH。
- 每个服务器 run 都要有独立 `run_id`、日志、完成标记、manifest。
- 大文件、samples、data、logs 不提交；只提交小体积 `manifest.json`、`metrics/`、汇总结果和文档。
- 收尾门禁按仓库约定执行：`ruff check . && black --check . && pytest -q`。文档-only 修改可视情况只检查 `git status`。

## 3. 已完成内容

### 3.1 研究流水线骨架

已搭建或完善以下研究层能力：

- 结构指标聚合：`research/eval/metrics.py`
- 实验评测入口：`research/scripts/eval_experiment.py`
- 实验运行脚本已接入结构合法率、repair distance、bootstrap CI 等指标。
- 外部 HF benchmark 适配骨架：`research/datasets/external.py`
- 官方 8 任务校准脚本：`research/scripts/run_calibration_full.sh`
- VAE-only GT encode/decode 对照脚本：
  - `research/scripts/vae_roundtrip.py`
  - `research/scripts/run_vae_only.sh`
- 服务器环境补充：`research/scripts/remote/server_bootstrap.sh` 已包含服务器侧 `datasets==4.4.2`。
- 同步检查：`research/scripts/remote/check_sync.sh` 已忽略服务器运行目录，如 `.venv-gpu/`。

### 3.2 9.2 预热对齐校准

官方 8 任务校准 `calib_official8` 已完成并通过：

- 结果目录：`research/results/calib_official8/`
- 论文参考平均值：`26.75`
- 本次复现平均值：`26.83`
- delta：`0.08`
- 容差：每任务 `3.0`
- 判定：`PASS`

任务结果：

| task | accuracy |
| --- | ---: |
| lambada | 50.90 |
| mmlu | 19.50 |
| obqa | 23.00 |
| hellaswag | 11.00 |
| race | 20.00 |
| siqa | 29.20 |
| squad | 30.30 |
| story_cloze | 30.77 |
| tasks_average | 26.83 |

9.2 预览实验也已完成：

| run_id | 结论 |
| --- | --- |
| `dyck1_L32_preview` | Cola 无条件 Dyck-1 L=32 合法率 `0.953125`，CI `[0.9140625, 0.984375]` |
| `dyck1_L32_completion_preview` | 条件补全合法率 `0.03125`，CI `[0.0078125, 0.0625]`，补全模式明显更差 |
| `json_smoke` | JSON parse/schema 合法率 `0.0`，均为 parse error |
| `dyck1_L32_preview_vae` | VAE-only GT roundtrip 合法率 `1.0`，`vae_ppl ~= 1.00017` |
| `json_smoke_vae` | VAE-only GT roundtrip 合法率 `1.0`，`vae_ppl ~= 1.02090` |

当前解释：这些小样本上，VAE 编解码器本身能保持结构，主要结构错误更像来自 Cola 扩散先验、采样或 prompt/条件补全链路。

### 3.3 9.3 Dyck 主诊断初版

已完成并归档：

| run_id | 数据/设置 | 结果 |
| --- | --- | --- |
| `dycklanguage_cola` | HF `lighteval/DyckLanguage`，config `2`，test split | n=`768`，valid_rate=`0.0`，mean_repair_distance=`29.154947916666668` |
| `dyck_L64_D8_k1_blk16` | 合成/可控 Dyck L64 D8 k1，block 16，多 seed | valid_rate=`0.009114583333333334`，CI `[0.0026041666666666665, 0.016927083333333332]`，mean_repair_distance=`29.139322916666668` |

`dyck_L64_D8_k1_blk16` seed 分解：

- seed `1234`：`0.01953125`
- seed `2025`：`0.0078125`
- seed `3407`：`0.0`

### 3.4 9.4 sweep 当前进度

服务器上 9.4 sweep 批处理已运行完成，但结果尚未拉回本地、尚未提交。

服务器日志与完成标记：

- 日志：`logs/dyck_9_4_sweeps.log`
- 完成标记：`logs/dyck_9_4_sweeps.done`
- 上次读取 `.done` 内容为 `0`，表示成功退出。

该批次顺序运行了：

- `dyck_L64_D8_k1_blk1`
- `dyck_L64_D8_k1_blk8`
- `dyck_L64_D8_k1_ode32`

上次可见日志显示 `dyck_L64_D8_k1_ode32` 已完成，valid_rate 约 `0.0065`。精确值以后续拉回的 `metrics/summary.json` 为准。

## 4. 尚未完成内容

### 4.1 9.4 待收尾

下一位 agent 首先应拉回并检查 9.4 三个 run：

```bash
REMOTE=cityu_tecent bash research/scripts/remote/fetch.sh dyck_L64_D8_k1_blk1
REMOTE=cityu_tecent bash research/scripts/remote/fetch.sh dyck_L64_D8_k1_blk8
REMOTE=cityu_tecent bash research/scripts/remote/fetch.sh dyck_L64_D8_k1_ode32
```

拉回后检查：

```bash
cat research/results/dyck_L64_D8_k1_blk1/metrics/summary.json
cat research/results/dyck_L64_D8_k1_blk8/metrics/summary.json
cat research/results/dyck_L64_D8_k1_ode32/metrics/summary.json
git status --short
```

只提交小体积结果：

- `research/results/<run_id>/manifest.json`
- `research/results/<run_id>/metrics/**`

不要提交：

- `research/results/<run_id>/data/**`
- `research/results/<run_id>/samples/**`
- `research/results/<run_id>/traces/**`
- 服务器 `logs/**`

### 4.2 9.4 仍缺的归因项

当前 9.4 只覆盖了一部分 block/ODE sweep。完整要求还包括：

- block 长度 sweep：`block in {1,4,8,16,...}`，目前已跑或待归档的是 `1/8/16`，仍缺 `4` 和更长档位（若资源允许）。
- latent 压缩率 sweep：尚未真正覆盖。
- ODE 步数 sweep：目前只有 `ode32` 作为一个补点，仍需更多步数。
- VAE-only 对照：9.2 spot check 已跑 Dyck/JSON 小样本，但 9.4 主网格下的 VAE-only 对照还未系统完成。
- 条件多峰性诊断与失配位置热力图：骨架/指标能力已有部分基础，但还未形成完整结论。

### 4.3 9.5 L1/L2 格式评测

尚未开始正式 GPU 跑：

- L1：`JSONSchemaBench` 必选
- L1 可选：`StructEval`
- L2：`BFCL`

相关配置和 adapter 已有骨架，但 JSONSchemaBench/BFCL 的实际字段还需要先做服务器侧 HF smoke 验证，再启动 GPU 任务。不要直接假设 adapter 已完全匹配数据列。

## 5. 常用操作流程

### 5.1 本地确认状态

```bash
git status --short
git rev-parse --short HEAD
```

### 5.2 服务器确认状态

```bash
ssh cityu_tecent 'cd /data0/users/siyuan/projects/Cola-DLM && git rev-parse --short HEAD'
ssh cityu_tecent 'nvidia-smi --query-gpu=index,memory.used,utilization.gpu --format=csv,noheader'
```

### 5.3 同步代码到服务器

```bash
REMOTE=cityu_tecent bash research/scripts/remote/sync.sh
REMOTE=cityu_tecent bash research/scripts/remote/check_sync.sh
```

注意：`sync.sh` 过去偶尔会打印 `cannot delete non-empty directory: research/results/smoke_dyck1`，只要最后 `check_sync.sh` 通过，一般可视为非致命。

### 5.4 detached 启动服务器任务

优先使用以下模式，避免本地 SSH 挂住：

```bash
ssh -n cityu_tecent 'cd /data0/users/siyuan/projects/Cola-DLM && mkdir -p logs && rm -f logs/<run_id>.done && (setsid bash -lc '\''source .venv-gpu/bin/activate
export DIT_PATH=hf_models/cola_dlm/cola_dit
export VAE_PATH=hf_models/cola_dlm/cola_vae
export TOKENIZER_PATH=hf_models/tokenizer.json
export NUM_GPUS=8
bash research/scripts/submit_job.sh research/configs/experiments/<config>.yaml
echo $? > logs/<run_id>.done'\'' > logs/<run_id>.log 2>&1 < /dev/null &); echo launched'
```

VAE-only 使用：

```bash
ssh -n cityu_tecent 'cd /data0/users/siyuan/projects/Cola-DLM && mkdir -p logs && rm -f logs/<run_id>.done && (setsid bash -lc '\''source .venv-gpu/bin/activate
export VAE_PATH=hf_models/cola_dlm/cola_vae
export TOKENIZER_PATH=hf_models/tokenizer.json
export CUDA_VISIBLE_DEVICES=0
bash research/scripts/run_vae_only.sh research/configs/experiments/<config>.yaml
echo $? > logs/<run_id>.done'\'' > logs/<run_id>.log 2>&1 < /dev/null &); echo launched'
```

### 5.5 查看服务器任务

```bash
ssh cityu_tecent 'cd /data0/users/siyuan/projects/Cola-DLM && cat logs/<run_id>.done 2>/dev/null || echo running && tail -80 logs/<run_id>.log'
```

如果 `.done` 为 `0`，说明脚本成功退出。如果没有 `.done`，再看日志尾部和 GPU 占用。

### 5.6 拉回结果

```bash
REMOTE=cityu_tecent bash research/scripts/remote/fetch.sh <run_id>
```

拉回后优先看：

```bash
cat research/results/<run_id>/manifest.json
cat research/results/<run_id>/metrics/summary.json
```

### 5.7 本地门禁

```bash
.venv/bin/ruff check .
.venv/bin/black --check .
.venv/bin/python -m pytest -q
```

本地环境是纯 Python 测试环境；不要安装模型依赖闭包。

## 6. 已知坑与处理方式

- 旧的 `nohup`/`setsid` 提交方式可能已经在服务器启动作业，但本地 SSH 仍卡住。此时只杀本地 SSH 客户端进程，不要杀服务器作业。
- 定位本地卡住的 SSH：

```bash
ps -ef | rg "ssh cityu_tecent.*<run_id>"
```

- 只 kill 本地 SSH PID，不能误杀服务器上的 Python/torchrun 进程。
- 服务器访问 GitHub/HF 不一定稳定；代码用 rsync 同步，HF 资源必要时走 `HF_ENDPOINT=https://hf-mirror.com`。
- 不要在服务器做 `apt upgrade`、NVIDIA driver、fabricmanager 等系统级改动。
- `lighteval/DyckLanguage` 已确认 configs 为 `['2', '3', '4']`；train split 很小，test split 有 500 条。adapter 当前用 `input` 作为 prompt/prefix，`output` 作为 ground_truth。
- JSONSchemaBench/BFCL adapter 还需要实际字段 smoke test 后再正式跑 9.5。

## 7. 建议的下一步顺序

1. 拉回 9.4 三个 sweep 结果：`blk1`、`blk8`、`ode32`。
2. 检查并记录每个 `metrics/summary.json`，确认 `.done=0` 与 manifest 完整。
3. 只提交小体积 metrics/manifest，并在提交信息里说明是 9.4 初始 sweep 归档。
4. 根据结果补齐 9.4 缺口：至少 block `4`，然后决定是否继续更长 block、更多 ODE steps、VAE-only 主网格。
5. 做 JSONSchemaBench 和 BFCL 的 HF 字段 smoke test。
6. 启动 9.5 L1/L2 baseline：先 JSONSchemaBench，再 BFCL；格式合法率和语义指标严格分开。
7. 汇总 9.2-9.5 的 manifest、配置哈希、commit、seed、checkpoint hash、指标和时间戳，确保满足 7.10 可复现要求。

## 8. 提交规范提醒

提交信息保持语义清晰、可回滚。按仓库约定，commit message 末尾附：

```text
Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```

